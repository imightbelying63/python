"""this file is only needed for testing scripts; it import modules needed by those scripts
   it should be automatically loaded by a unit test when run separately as __main__
   """
import re, subprocess
